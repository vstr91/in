{{ form_start(form, {'action': path('site_admin_banners_cadastra', {'id_banner': banner.id})}) }}
    {{ form_widget(form) }}
    <p>
        <input type="submit" class="d-none" id="btn-submit" value="Cadastrar" />
    </p>
{{ form_end(form) }}
<script type="text/javascript">
    {#$('#form-parada').submit(function(){
        $(this).ajaxSubmit(function(data){
            console.log(data);
            $(".caixaForm").dialog('close');
            return false;
        });
        return false;
    });#}
    
    $('#btn-editar').click(function(){
        $('#btn-submit').click();
    });
</script>